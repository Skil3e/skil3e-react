---
name: MenuToggle
menu: 2. Components
route: /components/menu-toggle
---

import { Playground, Props } from 'docz'
import MenuToggle from "./MenuToggle.tsx";
import "skil3e-css/src/components/_menu-toggle.scss";

<Playground>
{() => {
    const [isOpen, setIsOpen] = React.useState(false)
    function toggle() {
        setIsOpen(!isOpen)
    }
    return <MenuToggle isOpen={isOpen} onClick={toggle} />
}}  
</Playground>

## Props
<Props of={MenuToggle} />

